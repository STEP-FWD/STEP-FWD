# Logbook entry 
Author: BT
Date: April 8 2026

### Testing the two approaches for STEP formatting

1) Yaml file: 

It's quite easy to convert from ppt to yaml file with AI help (available to all now).

I converted the table easily, with no need of rereading the structure. 

I tried to make the webpage to check the result. I noticed I had also made changes to other protocols.
(I have added a minor fix to the yaml to md converter) -> I add a gitignore with the whole folder *site*. 

I make the webpage, it looks good. The md file can be also read in *docs*. The *site* folder is just a repetition of that + images and html stuff. 

I want to see if I can run from online github the action of converting from yaml to md. 


1) I made a workflow that converts the yaml to md file automatically when a commit is done. It can be found [here](.github/workflows/yaml-to-md.yml)

2) I tested  the workflow with a test yaml file that I modified online on github, and I committed. The md file was automatically generated in the correct folder. 

3) i cannot use github pages because Ediz is using it already :) It should work for the same reason

Conclusions: 

It is possible to run yaml from github, no need to have Python installation.

It is still not very userfriendly- but now AI can be used to fix it. I see it as a minor issue. 

In terms of compiling: I go now to check Ediz approach. I like yaml files for now. 



